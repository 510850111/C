#include<windows.h>
#include <stdio.h>
long fact(int n);

int main()
{
    int num;
    long ret;
    scanf("%d",&num);
    ret=fact(num)
    if(1==ret)
        {
          printf("error!\n");      
        }
    else
        {
                
        }
    
    printf("%ld	\n", num);

    return 0;
}
